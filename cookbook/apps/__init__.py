"""Cookbook flagship apps — realistic, multi-feature applications.

Each app is a package exposing ``build(model=None)`` and ``async run(...)``, with
a ``__main__`` so it runs as ``python -m cookbook.apps.<name>``, a ``README.md``
with real output, and a ``data/`` folder. Added in later waves.
"""
