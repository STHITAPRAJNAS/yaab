"""YAAB cookbook — mature, runnable examples for every major capability.

Each recipe (``cookbook/recipes/<name>.py``) and app (``cookbook/apps/<name>/``)
runs fully offline with a deterministic ``TestModel`` (so it and its test work in
CI with no key) and runs live by setting ``YAAB_SAMPLE_MODEL``. See
``cookbook/README.md`` for the catalog.
"""
