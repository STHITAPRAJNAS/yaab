"""Cookbook recipes — one focused, runnable example per capability.

Each module exposes ``async run()`` returning a result and self-checks via
``cookbook._harness.expect``. Run one with::

    python -m cookbook.recipes.agents
"""
